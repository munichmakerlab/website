<div class="step-list">
  <ul>
    <slot />
  </ul>
</div>

<style>
  .step-list :global(ul) {
    list-style: none;
    padding: 0;
    margin: 1.5rem 0;
    counter-reset: step-counter;
  }
  .step-list :global(li) {
    position: relative;
    padding-bottom: 1.5em;
    padding-left: 2.75rem;
    counter-increment: step-counter;
  }
  .step-list :global(li::before) {
    content: '';
    position: absolute;
    left: 10px;
    top: 26px;
    bottom: 0;
    width: 2px;
    background-color: var(--link);
    opacity: 0.35;
  }
  .step-list :global(li:last-child::before) {
    display: none;
  }
  .step-list :global(li::after) {
    content: counter(step-counter);
    position: absolute;
    left: 0;
    top: 2px;
    width: 22px;
    height: 22px;
    background-color: var(--link);
    color: #fff;
    font-size: 0.7rem;
    font-weight: 700;
    border-radius: 50%;
    text-align: center;
    line-height: 22px;
  }
</style>
